//! Module for edge for incidence node and it's store

mod flatten;
mod item;
mod item_store;

pub(in crate::graph) use flatten::*;
pub(in crate::graph) use item::*;
pub(in crate::graph) use item_store::*;
